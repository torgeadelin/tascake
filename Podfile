# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Tascake' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Tascake
  pod 'AWSCore', '~> 2.10.0'

  target 'TascakeTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'TascakeUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
